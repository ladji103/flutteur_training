import 'package:flutter/material.dart';

class MaxitAppBar extends StatelessWidget {
  const MaxitAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Max it OLIB',
      style: TextStyle(color: Colors.black),
    );
  }
}
